FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/config.py .
COPY src/detection/realtime_detector.py .

EXPOSE 8080

CMD ["python3", "realtime_detector.py"]
